# ── Xử lý dữ liệu ────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
pyarrow==18.1.0
unidecode==1.3.8
rapidfuzz==3.10.1

# ── Machine Learning ─────────────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
optuna==4.1.0

# ── Dự báo chuỗi thời gian ───────────────────────────────────────
prophet==1.1.6
# Ghim cmdstanpy: bản 1.3.x siết kiểm tra thư mục CmdStan và đòi có 'makefile',
# nhưng wheel của prophet chỉ đóng gói bản CmdStan rút gọn (chỉ có bin/ và
# stan/). Hệ quả: Prophet() ném AttributeError "no attribute 'stan_backend'"
# — thông báo hoàn toàn không liên quan tới nguyên nhân thật.
cmdstanpy==1.2.4
# torch cài riêng trong Dockerfile (bản CPU-only, tránh kéo ~2.5GB wheel CUDA)

# ── MLOps ────────────────────────────────────────────────────────
mlflow==2.19.0
boto3==1.35.81

# ── Kết nối hạ tầng ──────────────────────────────────────────────
psycopg2-binary==2.9.10
sqlalchemy==2.0.36
redis==5.2.1
hdfs==2.7.3
python-dotenv==1.0.1

# ── Biểu đồ cho báo cáo ──────────────────────────────────────────
matplotlib==3.9.3
seaborn==0.13.2
